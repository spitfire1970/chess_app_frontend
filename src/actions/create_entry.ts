"use server"

type Player = {
    rating: number,
    player: string
}

enum Success {
    Success,
    Fail
}

const async create_entry: Array<Player> = (username: string) => {
    const await fetch('')
    return () 
}

export create_entry